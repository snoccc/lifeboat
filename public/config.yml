all:
  file:
    - file -b "$file"
  mime-type:
    - file -b --mime-type "$file"
  # strings:  
  #   - strings "$file"
  # binwalk:
  #   - binwalk "$file"
mime-types:
# contents for text files should come in preview
  image:
    exif:
    - exiftool "$file"
  # - repair
  audio:
    - echo audio
  video:
    - echo video
extensions:
  .pcap:
    networkminer:
      - networkminer
    testList:
      - testList
  .apk:
    unpacked:
      - unpack-apk  

packages:
  - find-outdated
pwn:
  - checksec
  - dogbolt-main
  - get-template
crypto:
  - export2john
  - detect-crypto
  - xorsearch
unknown:
  - virustotal
